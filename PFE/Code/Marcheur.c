#include stdio.h
#define N 100

float F[N];
float Produit[N][N]
/* ModeleSimple du marcheur 
Ce code rend compte du premier modele ou unqiement la distance importe
*/
float ModeleSimple(int Noperation)
{ /* e[0] contient le rayon de e*/
int i,j, nbrProduit;
float F=0.5, WAF;

for(i=1;i<=NOperation;i++)
  {
    Fproduit = Fproduit * F;
  }
WAF=1-Fproduit;
return WAF;